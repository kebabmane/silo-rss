# Copy this file to docker.env (or another name) and supply real values.
# RAILS_MASTER_KEY must match the key stored in config/master.key on the host machine.
RAILS_MASTER_KEY=replace-with-your-master-key

# SECRET_KEY_BASE can be generated via `bin/rails secret`.
SECRET_KEY_BASE=replace-with-secret-key-base
