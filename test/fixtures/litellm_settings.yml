# LiteLLM Settings is a singleton, so only one fixture
default:
  server_url: "http://localhost:4000"
  default_model: "gpt-3.5-turbo"
  max_tokens: 4000
  temperature: 0.7
  timeout: 30
  enabled: false
